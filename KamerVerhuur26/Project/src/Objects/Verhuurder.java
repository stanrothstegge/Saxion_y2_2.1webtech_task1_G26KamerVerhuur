package Objects;

/**
 * Deze class representeerd een gebruiker in de rol van verhuurder.
 */
public class Verhuurder extends Gebruiker {
    public Verhuurder(String firstName, String lastName, String userName, String password) {
        super(firstName, lastName, userName, password);
    }
}
